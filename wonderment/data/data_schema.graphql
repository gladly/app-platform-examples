type Shipment {
    carrierDisplayName: String!
    status: String!
    trackingCode: String!
}

type Order @dataType(name: "wonderment_order", version: "1.0") {
    id: ID!
    name: String!
    shipments: [Shipment!]!
}

type Query {
    orders: [Order]
}